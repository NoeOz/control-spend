import { Text, View } from "react-native";
import { globalStyles } from "../styles/styles";

const ManageSpend = () => {
  return (
    <View style={globalStyles.principalContainer}>
      <Text>Adiministrar Subs</Text>
    </View>
  );
};

export default ManageSpend;
